- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.editorconfig:
    ~/.ackrc:
    ~/.config/nvim/init.lua: init.lua
    ~/.tern-config:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
