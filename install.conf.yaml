- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.editorconfig:
    ~/.ackrc:
    ~/.SpaceVim.d/autoload/config.vim: config.vim
    ~/.SpaceVim.d/plugin/coc.vim: coc.vim
    ~/.SpaceVim.d/coc-settings.json: coc-settings.json
    ~/.tern-config:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
